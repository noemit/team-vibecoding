# Copy this file to .env.local and fill in values (optional).
# The site stays public if PASSWORD is left empty.

# Set a password to lock the whole site behind a password page.
PASSWORD=

# API key applets can use to call an AI provider (OpenAI, Anthropic, ...).
LLM_API=
